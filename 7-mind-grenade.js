//node exports module automatically through function
//secondly, that function must be executed.
//Built-in Modules- OS(Operating System Module), PATH, FS(File System), HTTP(to set up http server)

const num1 = 5
const num2 = 10

function addValues(){
    console.log(`the sum is : ${num1 + num2}`);
}

addValues()